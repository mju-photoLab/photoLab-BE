cloud:
  aws:
    credentials:
      access-key: ${AWS_ACCESS_KEY_ID}
      secret-key: ${AWS_SECRET_ACCESS_KEY}
    s3:
      bucket: ${s3.bucket}
    region:
      static: ${AWS_DEFAULT_REGION}
    stack:
      auto: false